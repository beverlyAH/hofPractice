/*
 *
 *  _.each
 *
 */

// use _.each to double each number in the array.
var doubleNumbers = function(numbers) {

}

// use _.each to traverse the number array and determine
// which are multiples of five.
var multiplesOfFive = function(numbers) {

}




/*
 *
 *  _.filter
 *
 */

// use _.filter to return the fruits array with only lemon.
var onlyLemons = function(fruits) {

}

var startsWithP = function(fruits) {

}

// return a filtered array containing only cookie-type desserts.
var cookiesOnly = (desserts) => {

}

/*
 *
 *  _.reduce
 *
 */

// return the total price of all products.
var sumTotal = function(products) {

}

// return an object consisting of dessert types and how many of each.
// exampleOutput: { dessertType: 3, dessertType2: 1 }
var dessertCategories = function(desserts) {

}

/*
Use _.reduce to return a nested array, consisting of arrays contained two elements;
the first representing an ingredient, and the second how many times it appears across
all recipes.
Example: [['sugar', 5], ['flour', 4], ['water', 3]]
*/
var ingredientInventory = function(desserts) {

}

/*
 *
 *  _.map
 *
 */

// use _.map to return an array of items with their sale prices. round any decimals to two places.
/* example output:
  var salePrices = applyCoupon(grocery, 0.20);
  [
    {
      id: 1,
      product: 'Pork - Loin, Boneless',
      price: 6.90 // original price 8.63
    }
  ];
*/
  var applyCoupon = function(grocery, coupon) {

  }

